const sendiri = {
  firstName: "syauqi",
  lastname: "azka",
  age: 15,
};

sendiri.hobi = "cihuy"

console.log(`${sendiri.firstName} ${sendiri.lastname} ${sendiri.age} ${sendiri.ismarried} ${sendiri.hobi}`);